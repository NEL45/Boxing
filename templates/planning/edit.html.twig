{% extends 'base.html.twig' %}

{% block title %}Edit Planning{% endblock %}

{% block body %}
<div class="d-flex justify-content-center">
    <div class="planning-admin row col-lg-6 col-md-8 mb-4 bg-secondary text-center">
    <h1 class="text-center mt-5">Edit Planning</h1>

    {{ include('planning/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary text-light" href="{{ path('planning_index') }}">back to list</a>

    {{ include('planning/_delete_form.html.twig') }}
</div>
    
{% endblock %}
