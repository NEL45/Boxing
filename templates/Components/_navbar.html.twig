<nav class="navbar navbar-expand-lg fixed-top">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('accueil') }}">
			<img class="bg-logo" src="{{ asset('build/images/boxing-logo.png') }}" alt="logo"></a>

		<p class="navbar-title col-5 text-center display-2">
			SHAPE AND BOXING
		</p>

		<button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler -icon"></span>
		</button>
		<div class="collapse navbar-collapse col-2" id="navbarNavAltMarkup">
			<div class="navbar-nav">
				
				{% if is_granted('IS_ATHENTIFICATED_FULLY') %}
					Bienvenue{{ app.user.name }}!
					<a class="nav-item nav-link active text-end col-2 fs-6 fw-bold" href="{{ path('app_login') }}">DECONNEXION</a>
				{% else %}
					<a class="nav-item nav-link active text-end col-2 fs-6 fw-bold" href="{{ path('app_login') }}">CONNEXION</a>
				{% endif %}
				<a class="nav-item nav-link active col-2 fs-6 fw-bold" href="{{ path('accueil') }}">ACCUEIL</a>
				<a class="nav-item nav-link active text-end col-2 fs-6 fw-bold" href="{{ path('licensees') }}">PLANNING</a>
				<a class="nav-item nav-link active text-end col-3 fs-6 fw-bold" href="{{ path('planning_index') }}">PLANNING ADMIN</a>
				<a class="nav-item nav-link active text-end col-2 fs-6 fw-bold" href="{{ path('boxeurs') }}">BOXEURS</a>
				<a class="nav-item nav-link active text-end col-3 fs-6 fw-bold" href="{{ path('users_index') }}">BOXEURS ADMIN</a>
			</div>
		</div>
	</p>
</nav>
